@startuml
start
:Пользователь нажимает стрелки клавиатуры;
while (Игра активна?) is (да)
  if (Нажата клавиша?) then (yes)
    :Обновить направление змейки в соответствии с нажатой клавишей;
    :Переместить змейку на один шаг в новом направлении;
    if (Змейка достигла яблока?) then (yes)
      :Обработать съедание яблока;
      :Сгенерировать новое яблоко;
    endif
    if (Змейка столкнулась с препятствием?) then (yes)
      :Завершить игру;
    endif
    if (Змейка взорвалась от бомбы?) then (yes)
      :Завершить игру;
    endif
  endif
endwhile (нет)
stop
@enduml